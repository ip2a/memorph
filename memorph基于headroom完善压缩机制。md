# memorph 基于 Headroom 完善压缩机制

## 0. 目标原文与执行纪律

### 0.1 当前长期目标

用户目标：

> 这个对我非常重要 我需要你把你的这个记录放到一个 新的文件里 就叫 memorph基于headroom完善压缩机制。md 的文件里 写清楚 写详细所有要做的工作 要写的非常非常详细 这个对我非常重要 所有能从headroom学习到的东西都要写清楚，然后准备开始给我依照这些md文件推进，每当你做了一次记忆整理的时候 都要去看一下我的目标是什么 不要丢失目标 给我持续推进 每完成一个小环节就给我做好一次git 这个非常非常重要，完成一部分就在这个md上标注一部分进展 开始执行

### 0.2 这个文件的定位

这个文件不是普通调研总结，而是后续推进 memorph 压缩机制的长期工程规格书和进度面板。

后续每次处理本目标时，必须先读本文件，确认：

- 当前目标是什么。
- 已完成了哪些小环节。
- 下一个最小可交付环节是什么。
- 哪些部分不能简化。
- 哪些 Headroom 思路是要学习的，哪些不要搬。

### 0.3 后续执行纪律

每完成一个小环节必须做三件事：

1. 更新本文件的“进展记录”。
2. 只提交本环节相关文件，不把用户已有无关改动带入提交。
3. 做一次 git commit，commit message 要能说明完成的小环节。

如果某一小环节涉及代码改动，还要补充：

- 对应测试。
- 测试命令和结果。
- 未能测试的原因。

---

## 1. 当前已确认事实

### 1.1 memorph 已经具备的基础

memorph 不是从零开始做压缩。当前代码里已经有一部分非常关键的基础：

- canonical 层已有 `EventBlock::Compressed`。
- 已有 `memorph-archive://...` 形式的 archive reference。
- 已有压缩 archive 的写入、加载、列表、恢复、展开能力。
- 已有 provider-native compression projection 的架构。
- Codex 已支持把 canonical compressed block 导出成 Codex 原生 `type: "compacted"` rollout item。
- Codex 已支持导入 `type: "compacted"` 并映射回 `EventBlock::Compressed`。
- workspace 已经是一等能力，session 列表、切换、Codex 修复逻辑都有 workspace 维度。

这说明当前缺失的不是“能不能表示压缩块”，而是：

> memorph 还不能主动从普通 canonical session 中选择一段正常历史，把它真实压缩成一个新的 `EventBlock::Compressed`，并把原文完整归档。

### 1.2 当前 memorph 压缩机制的位置

当前已有逻辑主要是：

- 对已有压缩块进行 preserve。
- 对已有 native compaction 进行 normalize。
- 需要时 expand compressed segment。
- target provider 导出时把已有 compressed block 投影成目标 provider 的格式。

当前缺失的主动压缩流程是：

```text
普通 canonical events
-> 选择可压缩 range
-> 生成摘要/压缩内容
-> archive 原始 events
-> 用 EventBlock::Compressed 替换原 range
-> 再交给 provider projection
```

### 1.3 Headroom 的定位

Headroom 是一个 runtime request compression layer，不是 session migration tool。

Headroom 的核心目标是：

```text
agent/app 发给 LLM 的请求
-> Headroom proxy/SDK 先压缩 tool outputs/logs/RAG/history/files
-> 再转发给模型
```

所以 Headroom 解决的是“在线请求送进模型前变小”，memorph 当前目标解决的是“跨 agent 会话中间态和会话文件迁移时变小”。

两者不是同一层，但 Headroom 的压缩原则可以学习。

---

## 2. Headroom 需要学习的内容

### 2.1 必须深学：CCR

CCR = Compress, Cache, Retrieve。

Headroom 的 CCR 含义：

```text
原始大内容
-> 压缩成短内容/marker
-> 原文存进本地 store
-> prompt 里带 hash marker
-> 模型需要细节时调用 retrieve tool
-> runtime 根据 hash 取回原文
```

Headroom Rust 里的关键点：

- 原文 payload 会计算 hash。
- hash 用 BLAKE3。
- marker 格式类似 `<<ccr:HASH>>`。
- store 可以是 memory/sqlite/redis。
- runtime 通过 `headroom_retrieve` 查回原文。

memorph 应该学习的是语义，不是照搬实现。

memorph 对应实现应该是：

```text
source events
-> 写入 durable compression archive
-> 得到 memorph-archive://...
-> compressed event 里带 archive_ref
-> CLI/API/MCP 可以按 archive_ref 取回原文
```

memorph 不应该照搬 Headroom 的 5 分钟 TTL cache，因为 session archive 需要长期稳定可恢复。

memorph 必须保证：

- 压缩块可逆。
- archive ref 稳定。
- 原始事件完整保存。
- restore/expand 不依赖 LLM。

### 2.2 必须深学：ContentRouter

Headroom 不把所有内容都交给一个摘要器，而是先识别内容类型，再选择策略。

Headroom 的内容类型大致包括：

- JSON array。
- tool output。
- logs/build/test output。
- search/ripgrep results。
- git diff。
- code。
- plain text。
- mixed content。

Headroom 的策略大致是：

```text
detect content type
-> route to SmartCrusher / LogCompressor / SearchCompressor / DiffCompressor / CodeCompressor / Kompress
-> 得到 compressed candidate
-> 验证是否真的更小
-> 接受或拒绝
```

memorph 应该学习这个分层：

```text
canonical event/block
-> 判断是 conversation / tool output / log / search result / code / provider payload
-> 使用对应压缩策略
```

memorph 不应该一开始就做大而全算法。第一阶段可以先做 deterministic router 的最小可用版本：

- conversation range：保留任务目标、关键决定、未解决问题、最后结论。
- tool/log：保留命令、退出状态、错误摘要、关键路径、最后 N 行、失败原因。
- search result：保留 query、命中文件列表、关键匹配行摘要。
- code/diff：默认保守，不主动压缩正文，只记录文件路径和变更摘要，除非后续有专门策略。

### 2.3 必须深学：live-zone 原则

Headroom 新版明确不再做 rolling window 式“删历史消息”，而是只压缩 live zone。

Headroom runtime 里的 live-zone 大意：

- 不动 provider prompt cache 热区。
- 不动 system/tool definitions/historical prefix。
- 只动最新请求中模型马上要用的大块内容。
- 失败时原文原样转发。

memorph 是 session-level，不是 request-level，所以映射方式不同：

memorph 的 live-zone-like 原则应该是：

- 默认不压最近 N 轮。
- 默认不压当前任务目标、最新用户问题、最新 assistant 结论。
- 优先压旧历史、重复 tool output、大日志、大搜索结果。
- 切换 agent 时，目标 agent 不应该看到大量已失效的原文历史。

这个原则不是为了“偷懒少压”，而是为了防止把当前任务状态压没。

### 2.4 必须深学：token validation gate

Headroom 每个 block 压缩后会检查：

```text
compressed_tokens < original_tokens
```

如果不满足，就记录 `RejectedNotSmaller`，保留原文。

memorph 也必须有类似 gate：

```text
estimated_compressed_size < original_size
```

建议第一阶段不要追求 provider 精确 tokenizer，可以先做两层：

1. 字符/字节级 size gate：压缩文本必须比原 canonical text 小。
2. 粗略 token estimate gate：按 3.5 或 4 chars/token 估算。

后续可以增加 provider-specific tokenizer。

必须拒绝的情况：

- 摘要太长。
- archive marker + metadata 之后没有节省。
- range 太小，不值得压。
- 压缩后无法生成足够有意义的 summary。

### 2.5 必须深学：manifest / decision report

Headroom 的每个 block 都会记录 outcome。

典型 outcome：

- `Compressed`：压缩成功。
- `RejectedNotSmaller`：压了但没变小，拒绝。
- `BelowByteThreshold`：太小，不压。
- `NoCompressionApplied`：没有适合的压缩器。
- `CompressorError`：压缩器失败，当前 block 回退。
- `Excluded`：属于 hot zone，不能动。

memorph 必须学习这个，因为用户明确不希望“没底线没有标准的简化”。

memorph 的 dry-run report 应该至少包括：

- candidate id。
- event range。
- source event ids。
- role 分布。
- 原始 size。
- 预计压缩后 size。
- 预计节省。
- 选择原因。
- 风险级别。
- 是否会 archive。
- 如果跳过，跳过原因。

### 2.6 必须深学：provider-specific projection

Headroom 分 provider 的原因不是每个 provider 用完全不同压缩算法，而是：

- 请求格式不同。
- live zone 定位不同。
- cache hot zone 不同。
- tool/result 结构不同。
- 认证/订阅策略不同。
- runtime 工具注册方式不同。

例如：

- Anthropic `/v1/messages`：内容在 `messages[].content[]`，有 cache_control。
- OpenAI Chat `/v1/chat/completions`：tool result 是 `role=tool` message。
- OpenAI Responses `/v1/responses`：内容是 typed input item。
- Codex wrap：需要改 `~/.codex/config.toml`，支持 WebSocket/HTTP 和 MCP。

memorph 对应 provider 差异应该是：

- source detection：能不能识别源 agent 的原生压缩块。
- normalize：源原生压缩块如何变成 `EventBlock::Compressed`。
- target projection：目标 agent 能否接收 native compressed block。
- portable fallback：不能 native 时怎么展示。
- retrieval support：目标 agent 能不能通过 MCP/API/CLI 找回 archive。
- workspace semantics：目标 agent 的 workspace/session index 怎么写。

memorph 不应该让每个 provider 自己做压缩算法。压缩算法应该在 canonical 层完成，provider adapter 只负责原生格式映射。

### 2.7 可浅学：byte-range surgery

Headroom 的 runtime proxy 为了不破坏 prompt cache，会做 byte-range surgery：

```text
out = original_body[..block_start] + replacement + original_body[block_end..]
```

这样不会因为 JSON reserialize 改变空格、key 顺序、数字格式。

memorph 当前处理 canonical session，不是在线 API body，所以第一阶段不需要这个。

只有未来 memorph 要做 runtime proxy 时，才需要深学。

### 2.8 可浅学：Headroom memory

Headroom 有 cross-agent memory，但它是长期记忆同步，不是 session compression。

对 memorph 有启发：

- 可以按 workspace 隔离记忆。
- 可以给 Codex 写 AGENTS.md。
- 可以做 agent-native memory adapter。

但这不是当前压缩主线。

### 2.9 不要学习/不要照搬

不要照搬：

- Python proxy 架构。
- runtime subscription/auth 代理细节。
- 5 分钟 TTL CCR cache 作为 session archive。
- 把压缩逻辑分散到每个 provider adapter。
- LLM-only compression。
- 无条件展开 compressed segments。

---

## 3. memorph 目标架构

### 3.1 总原则

memorph 的核心设计应该是：

```text
provider raw session
-> provider import
-> CanonicalSession
-> active_compression
-> CanonicalSession with EventBlock::Compressed
-> prepare_session_for_target_provider
-> provider export/native projection
```

压缩应该发生在 canonical 层。

provider adapter 不做主动压缩，只做：

- 识别原生压缩。
- 归一化为 canonical compressed block。
- 把 canonical compressed block 投影成目标 provider 原生格式。

### 3.2 新模块：`core/active_compression`

职责：

- 接收 `CanonicalSession`。
- 接收 compression policy。
- 调用 planner 找候选 range。
- 调用 strategy 生成 summary。
- 调用 archive writer 写原始 events。
- 用 compressed event 替换原 range。
- 生成 `ActiveCompressionReport`。

建议核心结构：

```rust
pub struct ActiveCompressionParams {
    pub source_provider_id: String,
    pub target_provider_id: String,
    pub policy: ActiveCompressionPolicy,
    pub dry_run: bool,
}

pub struct ActiveCompressionReport {
    pub candidates: Vec<CompressionCandidateReport>,
    pub applied_segments: usize,
    pub skipped_segments: usize,
    pub original_estimated_tokens: usize,
    pub compressed_estimated_tokens: usize,
    pub archive_refs: Vec<String>,
}
```

### 3.3 新模块：`core/compression_planner`

职责：

- 扫描 canonical events。
- 建立 event range candidates。
- 保护最近上下文。
- 跳过不可压缩内容。
- 给每个 candidate 生成原因。

第一阶段 candidate 类型：

- `HistoricalConversationRange`
- `LargeToolOutput`
- `LargeLogOutput`
- `SearchResults`
- `ProviderPayloadText`

第一阶段默认策略：

- 保护最近 4 到 8 个 message events。
- 不压 system/developer 指令。
- 不压当前最后一个 user event。
- 不压已经是 `EventBlock::Compressed` 的 event。
- 不压没有 archive 能力的内容。
- 大于阈值才进入候选。

### 3.4 新模块：`core/compression_strategy`

职责：

- 对 candidate 生成 compressed summary。
- 估算 before/after size。
- 返回是否可接受。

第一阶段策略：

#### 3.4.1 Conversation deterministic summary

保留：

- 起始用户目标。
- 关键任务目标变化。
- 用户明确要求。
- 已做决定。
- 已完成工作。
- 未完成工作。
- 涉及文件路径。
- 错误/阻塞。
- 最后状态。

不保留：

- 冗余寒暄。
- 重复确认。
- 大段已过期工具输出。
- 已经归档的细节。

#### 3.4.2 Tool/log reducer

保留：

- 命令或工具名。
- 是否成功。
- exit code。
- 关键 error/warn。
- 关键文件路径。
- 最后 N 行。
- output 总行数/字节数。

不保留：

- 大量重复安装日志。
- 重复构建进度条。
- 大量无意义 stack trace 中重复帧。

#### 3.4.3 Search result reducer

保留：

- 查询词。
- 命中文件。
- 关键匹配行。
- 总命中数量。

不保留：

- 大量重复上下文行。

### 3.5 新模块：`core/compression_estimator`

职责：

- 估算 canonical event/block size。
- 估算 compressed summary size。
- 提供 gate。

第一阶段可以用：

- UTF-8 byte count。
- char count。
- 简单 token estimate。

第二阶段再接 provider tokenizer。

### 3.6 新模块：`retrieval_bridge`

职责：

- 根据 `memorph-archive://...` 找 archive。
- 返回原始 events。
- 支持 CLI/API retrieval。
- 未来支持 MCP retrieval。

第一阶段先做 CLI/API 即可。

---

## 4. 主动压缩流程

### 4.1 dry-run 流程

```text
read/import session
-> canonical session
-> planner scan
-> produce candidates
-> estimate before/after
-> output report
-> do not modify session
```

dry-run 输出必须告诉用户：

- 哪些会压。
- 为什么会压。
- 哪些不会压。
- 为什么不会压。
- 预计节省多少。
- 压缩后如何恢复。

### 4.2 apply 流程

```text
read/import session
-> canonical session
-> planner scan or selected ranges
-> strategy summarize
-> validate size
-> write archive
-> replace events
-> write/export session
-> report archive refs
```

### 4.3 switch 集成流程

```text
source provider import
-> canonical session
-> active compression policy
-> compressed canonical session
-> prepare_session_for_target_provider
-> target provider export
```

默认 policy 应该是保守的：

- 如果 session 没超过阈值，不主动压。
- 如果 target provider 原生支持 compressed block，可以更积极。
- 如果 target provider 不支持 retrieval，只做 portable marker，不压太激进。

---

## 5. provider 差异设计

### 5.1 provider capability registry

需要有一个 registry 明确每个 provider 的能力。

建议字段：

```rust
pub struct CompressionProviderCapability {
    pub provider_id: &'static str,
    pub detects_native_source: bool,
    pub native_target_projection: bool,
    pub portable_target_projection: bool,
    pub retrieval_tool_support: RetrievalSupport,
    pub default_projection: CompressionProjection,
}
```

### 5.2 Codex

当前已知：

- Codex 原生 session rollout 有 `type: "compacted"`。
- memorph 已经能导入 `compacted` 为 `EventBlock::Compressed`。
- memorph 已经能导出 `EventBlock::Compressed` 为 `compacted`。

目标：

- active compression 产生的 compressed block 在 Codex 导出时必须继续变成 `type: "compacted"`。
- `replacement_history` 里应该是 model-visible summary。
- `payload.memorph` 里继续保留 archive_ref、source_event_ids、source_event_count。

### 5.3 Opencode

当前已知：

- memorph 已有 opencode source compression normalize。
- opencode 是 native source/target adapter 之一。

目标：

- active compression 不写 opencode 私有格式。
- active compression 先写 canonical compressed block。
- opencode adapter 再负责 native projection。

### 5.4 Claude

Claude 的 session 格式不是本阶段主动压缩的原生目标重点。

目标：

- 不要无条件 expand compressed segments。
- 如果 Claude 没有可写 native compaction 格式，先用 portable marker。
- 后续可研究 Claude Code 的 memory/MCP retrieval 接入。

### 5.5 Kimi / DeepSeek TUI

当前目标：

- 先保证它们不会把其他 agent 压缩过的无意义 marker 当普通上下文长文本展开。
- 能识别 canonical compressed block。
- target 不支持 native compaction 时使用 portable projection。

后续目标：

- 继续研究各自原生压缩/上下文机制。
- 如果有原生 compact marker，再新增 target projection。

---

## 6. 用户可选择压缩的设计

### 6.1 CLI

建议新增：

```text
memorph compression plan --session <id> --target codex
memorph compression apply --session <id> --target codex --candidate <id>
memorph compression apply --file session.morph --range <start..end>
```

必须支持：

- dry-run。
- apply。
- list candidates。
- include/exclude candidate。
- 输出 archive refs。

### 6.2 API

建议新增：

```text
POST /api/v1/compression/plan
POST /api/v1/compression/apply
POST /api/v1/compression/retrieve
```

### 6.3 TUI

建议后续做：

- 在 session detail 中展示压缩候选。
- 用户勾选 candidate。
- 展示 before/after summary。
- 确认 apply。

---

## 7. LLM 使用边界

### 7.1 不能交给 LLM 的部分

以下绝不能由 LLM 决定：

- 原文是否保存。
- archive id/ref。
- source event ids。
- provider native projection。
- restore/expand。
- token/size gate。
- 是否删除原始 event。

### 7.2 可以交给 LLM 的部分

LLM 只适合做：

- 压缩摘要。
- 任务状态提炼。
- 决策和待办提炼。
- 面向不同 agent 的摘要风格调整。

### 7.3 LLM summarizer 必须满足的标准

LLM summarizer 输出必须有结构：

```json
{
  "summary": "...",
  "goals": [],
  "decisions": [],
  "completed_work": [],
  "open_questions": [],
  "files": [],
  "risks": []
}
```

必须有 deterministic fallback。

LLM 输出必须通过：

- 非空检查。
- size gate。
- 必要字段检查。
- archive ref 附加。

---

## 8. 不允许的“没底线简化”

不允许：

- 只生成一段摘要，不保存原文。
- 只按字符数截断。
- 只保留最后几轮，把中间直接丢掉。
- 压缩失败仍然删除原文。
- 跨 agent 切换时把 compressed block 展开成巨大原文。
- 把所有 provider 都输出成同一段普通文本。
- 没有 dry-run、没有 report、用户不知道发生了什么。

允许的阶段性简化：

- 第一阶段不用 LLM。
- 第一阶段不用 provider tokenizer。
- 第一阶段不用 MCP retrieval。
- 第一阶段不用 runtime proxy。
- 第一阶段只实现 Codex native projection 验证，因为 Codex 已有基础。

这些简化不伤害核心闭环。

---

## 9. 实施阶段

### Phase 1：规格与现状确认

目标：

- 固化本文件。
- 明确 Headroom 可学习内容。
- 明确 memorph 不照搬内容。
- 明确小环节提交纪律。

验收：

- 本文件存在。
- 本文件包含目标、原则、架构、阶段、进展记录。
- git commit 已提交本文件。

### Phase 2：Active Compression 数据结构

目标：

- 新增主动压缩参数、报告、候选结构。
- 不改现有 provider 行为。

建议改动：

- `rust/crates/memorph/src/core/active_compression.rs`
- `rust/crates/memorph/src/core.rs` 或 `core/mod` 暴露模块。

验收：

- 能构造 dry-run report。
- 不改变现有 switch/export 行为。
- cargo test 通过。

### Phase 3：Planner MVP

目标：

- 从 `CanonicalSession` 中找候选 event ranges。
- 输出 selected/skipped reasons。

默认规则：

- 跳过 system/developer。
- 跳过最近 N 个 message events。
- 跳过已有 compressed block。
- 小于阈值跳过。
- 大 tool/log/search/conversation range 成为候选。

验收：

- 单元测试覆盖 selected/skipped。
- dry-run 能解释原因。

### Phase 4：Archive-backed apply

目标：

- 把 candidate 原始 events 写入 archive。
- 用 compressed summary event 替换。
- expand 能恢复。

验收：

- `compress -> expand` 后恢复原始 events。
- archive_ref 存在并可加载。

### Phase 5：Codex target 验证

目标：

- active compression 后导出 Codex。
- 确认输出 `type: "compacted"`。
- 确认 `payload.memorph.archive_ref` 存在。

验收：

- Codex export 测试通过。

### Phase 6：CLI/API dry-run

目标：

- 用户能看见候选压缩计划。
- 不需要先 apply。

验收：

- CLI/API 返回 candidates。
- 每个 candidate 有 reason、estimated savings、risk。

### Phase 7：CLI/API apply

目标：

- 用户可以选择 candidate apply。
- 输出 archive refs 和报告。

验收：

- apply 产生变小 session。
- expand 可恢复。

### Phase 8：TUI 用户选择

目标：

- 在 TUI 展示候选。
- 用户选择压缩。

验收：

- TUI 能预览并确认。

### Phase 9：Retrieval bridge

目标：

- CLI/API 根据 archive_ref 找回原始内容。
- 未来支持 MCP。

验收：

- `memorph-archive://...` 可解析并返回原始 events。

### Phase 10：LLM summarizer 可选后端

目标：

- 在 deterministic summary 稳定后再加。
- 不能破坏可逆性。

验收：

- LLM 失败 fallback。
- LLM 输出过长会 reject。

---

## 10. 每次继续推进前的检查清单

每次开始工作前检查：

- [ ] 已阅读本文件。
- [ ] 明确当前长期目标不是“简单总结”，而是完善压缩机制。
- [ ] 明确本次要完成的小环节。
- [ ] 明确不会提交无关用户改动。

每次完成小环节后检查：

- [ ] 更新本文件进展记录。
- [ ] 运行必要测试或说明未运行原因。
- [ ] git status 确认只提交相关文件。
- [ ] git commit。

---

## 11. 进展记录

### 2026-06-07：Phase 1 / 规格文件创建

状态：完成。

已完成：

- 创建本文件。
- 固化用户长期目标。
- 梳理 Headroom 可学习内容。
- 梳理 memorph 目标架构。
- 梳理不允许的简化和允许的阶段性简化。
- 梳理后续 Phase 2 到 Phase 10。

提交范围：

- 仅提交 `memorph基于headroom完善压缩机制。md`。
- 不提交其他已有工作区改动或忽略文件。

下一步：

- 进入 Phase 2：实现 Active Compression 数据结构。

### 2026-06-07：Phase 2 / Active Compression 数据结构

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认长期目标是“基于 Headroom 完善 memorph 压缩机制”，不是只做文档总结。
- 已确认本次小环节范围：只实现数据结构和 dry-run 报告构造，不接入 switch/export，不写 archive，不做 planner/apply。
- 已检查工作区状态，避免提交无关改动。

已完成：

- 新增 `rust/crates/memorph/src/core/active_compression.rs`。
- 在 `rust/crates/memorph/src/core.rs` 暴露 `active_compression` 模块。
- 新增主动压缩参数结构：
  - `ActiveCompressionParams`
  - `ActiveCompressionPolicy`
  - `ActiveCompressionMode`
- 新增报告结构：
  - `ActiveCompressionReport`
  - `CompressionCandidateReport`
  - `CompressionSkipReport`
- 新增决策枚举：
  - `CompressionCandidateKind`
  - `CompressionSelectionReason`
  - `CompressionSkipReason`
  - `CompressionRisk`
- 新增只读 dry-run 汇总函数：
  - `build_dry_run_report`
  - `estimate_session_bytes`
  - `estimate_tokens_from_bytes`
- 添加单元测试，确认：
  - dry-run report 可构造。
  - dry-run 不修改 session。
  - policy 字段序列化稳定。
  - 已有 compressed event 数量可统计。

明确未做：

- 未选择压缩 range。
- 未写 archive。
- 未替换 session events。
- 未接入 switch/export。
- 未引入 LLM。
- 未改变任何 provider 行为。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression`：通过，2 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，107 passed。

遇到的问题：

- `cargo fmt --manifest-path rust/Cargo.toml` 失败，原因是 workspace manifest 没有直接 target。
- 处理方式：改用 crate manifest `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`，已通过。

提交范围：

- `rust/crates/memorph/src/core/active_compression.rs`
- `rust/crates/memorph/src/core.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 3：Planner MVP，从 `CanonicalSession` 中找候选 event ranges，并输出 selected/skipped reasons。

### 2026-06-07：Phase 3 / Planner MVP

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认当前目标是持续推进 memorph 的主动压缩机制。
- 已确认 Phase 3 边界：只做候选规划和 skipped reason，不写 archive，不替换 events，不接入 switch/export。
- 已检查工作区状态。

已完成：

- 在 `build_dry_run_report` 中接入 planner，dry-run report 现在会包含：
  - `candidates`
  - `skipped`
  - `estimated_bytes_saved`
  - `estimated_tokens_saved`
  - `compressed_estimated_bytes`
  - `compressed_estimated_tokens`
- 新增 `plan_compression_candidates`。
- 新增最近消息保护逻辑：
  - 默认保护最近 `protect_recent_message_events` 个 message events。
- 新增 skip rules：
  - system/developer 指令跳过：`SystemOrDeveloperInstruction`
  - 已有压缩块跳过：`AlreadyCompressed`
  - 最近消息跳过：`ProtectedRecentMessage`
  - 小于阈值跳过：`BelowByteThreshold`
  - 估算节省率不足跳过：`InsufficientEstimatedSavings`
  - 暂不支持的 event shape 跳过：`UnsupportedEventShape`
- 新增 candidate classification：
  - `HistoricalConversationRange`
  - `LargeToolOutput`
  - `LargeLogOutput`
  - `SearchResults`
  - `ProviderPayloadText`
- 新增保守估算：
  - tool/log 预计保留 20%
  - search 预计保留 30%
  - historical conversation 预计保留 35%
  - provider payload 预计保留 50%
  - summary 下限 128 bytes，避免过度乐观。
- 新增测试覆盖：
  - 大 conversation/tool/log/search 会进入 candidates。
  - system、已压缩、小内容、最近消息会进入 skipped。
  - 高 savings threshold 会拒绝候选。

明确未做：

- 还没有多事件 range 合并。
- 还没有实际摘要生成。
- 还没有 archive-backed apply。
- 还没有删除或替换任何原始 event。
- 还没有 CLI/API/TUI 暴露。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression`：通过，4 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，109 passed。

遇到的问题：

- 第一次运行 `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression` 时，`planner_selects_large_candidates_and_reports_skips` 失败。
- 原因：测试样例里的 conversation/search 内容太短，保守 summary 下限 128 bytes 导致估算节省率不足，被 planner 正确拒绝。
- 处理方式：不放宽规则，改为使用真正的大内容样例，测试通过。

提交范围：

- `rust/crates/memorph/src/core/active_compression.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 4：Archive-backed apply，把 selected candidate 的原始 events 写入 archive，并用 `EventBlock::Compressed` 替换。

### 2026-06-07：Phase 4 / Archive-backed Apply

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是“真实写 archive 并替换 session events”，不是只做 planner 或报告。
- 已确认边界：不接入 CLI/API/TUI，不改变 provider switch/export 行为，不引入 LLM，不做多事件 range 合并。
- 已检查工作区状态，当前相关变更只集中在 active compression 与 compression archive helper。

已完成：

- 新增主动压缩 apply 参数和结果结构：
  - `ActiveCompressionApplyParams`
  - `ActiveCompressionApplyResult`
- 新增 apply 入口：
  - `apply_active_compression`
  - `apply_active_compression_with_archive_dir`
- apply 流程已经形成最小闭环：
  - 先复用 `build_dry_run_report` 得到候选。
  - `candidate_ids` 为空时默认选择全部候选。
  - `candidate_ids` 非空时只压缩指定候选。
  - 每个 selected candidate 的原始 `SessionEvent` 先写入 compression archive。
  - archive 写入成功后，才用 `EventBlock::Compressed` 替换原 event。
  - compressed block 记录 `source_provider_id`、`source_event_ids`、`source_event_count`、`archive_ref`。
  - deterministic summary 会包含 candidate kind、reason、source ids、估算大小、preview、archive ref。
  - report 会回填 `archive_refs`。
- 在 `core::compression` 中新增 archive 写入复用 helper：
  - `write_active_compression_archive_in_dir`
- 在测试范围内新增 archive 展开 helper：
  - `expand_compressed_segments_in_dir`
  - 该 helper 标记为 `#[cfg(test)]`，避免扩大生产 API 面和 dead code warning。
- 添加测试覆盖：
  - apply 会写 archive，并用 `EventBlock::Compressed` 替换指定 candidate。
  - compressed block 的 `source_provider_id` 使用 apply 参数中的源 provider，例如 `claude`，不是硬编码 `memorph`。
  - archive 可展开恢复原始 event。
  - 无候选时 apply 返回原 session，不写 archive。

明确未做：

- 还没有 CLI/API/TUI 暴露。
- 还没有接入 session switch 或 provider export 流程。
- 还没有 LLM 摘要生成。
- 还没有 Headroom 风格的内容专用压缩器。
- 还没有多事件 range 合并。
- 还没有用户选择 UI。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression`：通过，6 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，111 passed。

遇到的问题：

- 完整测试首次通过时出现 `expand_compressed_segments_in_dir` dead code warning。
- 原因：该 helper 只用于单元测试验证 archive 可恢复，不应该暴露为生产路径。
- 处理方式：加 `#[cfg(test)]`，完整测试重新通过且无 warning。

提交范围：

- `rust/crates/memorph/src/core/active_compression.rs`
- `rust/crates/memorph/src/core/compression.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 5：Codex target verification，验证主动压缩后的 canonical compressed block 在导出到 Codex 时会变成 Codex 原生 `compacted`，并且从 Codex 再导入时仍能映射回 canonical compressed block。

### 2026-06-07：Phase 5 / Codex target verification

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 5 的目标是验证 active compression 的产物能正确投影到 Codex native compaction。
- 已检查当前工作区状态，开始时无未提交改动。
- 已检查现有 Codex compression 测试，确认已有覆盖是“手写 compressed block -> Codex compacted”，缺少“active apply 产物 -> Codex compacted”的端到端覆盖。

已完成：

- 在 `rust/crates/memorph/src/providers/codex.rs` 新增测试：
  - `active_compression_export_round_trips_as_native_codex_compacted_rollout`
- 测试覆盖真实流程：
  - 构造 Claude 来源 canonical session。
  - 使用 `apply_active_compression_with_archive_dir` 真实执行 active compression。
  - 确认 apply 产生 `archive_ref`。
  - 使用 `export_canonical_session_in_codex_dir` 导出到 Codex rollout。
  - 确认导出的 rollout 中存在 Codex 原生 `type: "compacted"` 行。
  - 确认 `payload.memorph.source_provider_id == "claude"`。
  - 确认 `payload.memorph.source_event_ids` 包含被压缩的原 event。
  - 确认 `payload.memorph.archive_ref` 等于 active compression 生成的 archive ref。
  - 确认 `replacement_history` 的 model-visible 文本包含 compressed marker 和 archive ref。
  - 确认旧原文没有作为普通 `response_item` 被导出给 Codex。
  - 再用 `import_canonical_session` 导入导出的 Codex rollout。
  - 确认导入后仍是 `EventBlock::Compressed`，并保留 source provider、source event ids、archive ref。

明确未做：

- 没有改 Codex 生产导出逻辑，因为现有实现已经支持 native compaction projection。
- 没有接入 session switch。
- 没有新增 CLI/API/TUI。
- 没有引入 LLM。
- 没有扩展其他 provider 的 active compression target 验证。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_export_round_trips_as_native_codex_compacted_rollout`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib providers::codex::tests`：通过，14 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，112 passed。

遇到的问题：

- 本阶段没有发现生产逻辑缺陷。
- 结论：Codex native projection 现有架构能优雅承接 active compression 产物，Phase 5 主要补强了验收证据。

提交范围：

- `rust/crates/memorph/src/providers/codex.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 6：CLI/API dry-run，让用户能在不 apply 的情况下查看 active compression candidates、skips、estimated savings、risk。

### 2026-06-07：Phase 6 / CLI API dry-run

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 6 的目标是“用户能看见候选压缩计划，不需要先 apply”。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段不做 apply，不写 archive，不改 provider switch/export 行为。

已完成：

- 新增 core 公共入口：
  - `ActiveCompressionDryRunParams`
  - `active_compression_dry_run`
- dry-run 输入支持两种来源：
  - `session_id`：从 `source_provider_id` 对应 provider 原生会话加载。
  - `file`：从 `.json/.md/.html/.morph` canonical export 文件加载。
- dry-run 参数支持：
  - `source_provider_id`
  - `target_provider_id`
  - `policy`
  - `session_id`
  - `file`
- 新增 API 路由：
  - `POST /api/v1/compression/plan`
- API body 支持：
  - `source_provider_id`
  - `target_provider_id`
  - `session_id`
  - `file`
  - `policy`
- API 返回完整 `ActiveCompressionReport`，包含：
  - `candidates`
  - `skipped`
  - `reason`
  - `estimated_bytes_saved`
  - `estimated_tokens_saved`
  - `risk`
- 新增 CLI 命令：
  - `memorph compression plan SOURCE TARGET --session-id SESSION_ID`
  - `memorph compression plan SOURCE TARGET --file session.json`
- CLI policy 参数：
  - `--protect-recent-message-events`
  - `--min-candidate-bytes`
  - `--min-savings-ratio-percent`
- CLI dispatch 已接入 `rust/apps/memorph/src/main.rs`，会输出：
  - 原始/压缩后估算大小。
  - candidates 列表。
  - skipped 列表。
  - 每个 candidate 的 reason、risk、estimated savings。
- 新增测试覆盖：
  - core 从 canonical file dry-run 返回 candidates 和 skipped reason。
  - API route 从 canonical file dry-run 返回 candidates、reason、risk、estimated savings。
  - CLI parser 接受 `compression plan` 的 file 和 policy 参数。
  - app package 编译通过，确认 CLI dispatch 可编译。

明确未做：

- 没有实现 CLI/API apply。
- 没有写 archive。
- 没有修改 session。
- 没有接入 TUI。
- 没有引入 LLM。
- 没有 provider-specific tokenizer。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_dry_run_from_file_returns_candidates_and_skips`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_plan_route_returns_candidates_from_file`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_plan_accepts_file_and_policy_options`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，115 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。

遇到的问题：

- `memorph-lib` crate 只定义 CLI schema，实际 CLI dispatch 在 `rust/apps/memorph/src/main.rs`。
- 处理方式：同时更新 `cli.rs` 和 app dispatch，避免只做 parser 不做实际命令。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 7：CLI/API apply，让用户可以选择 candidate_ids 执行主动压缩，并返回 archive refs 与压缩报告。

### 2026-06-07：Phase 7 / CLI API apply

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 7 的目标是“用户可以选择 candidate apply，并输出 archive refs 和报告”。
- 已确认本阶段边界：apply 写出压缩后的 canonical export，不直接修改 provider 原生会话仓库；provider 原生投影仍由后续 export/switch 流程负责。
- 已检查工作区状态，当前相关变更集中在 core apply command、CLI、API、app dispatch。

已完成：

- 新增 core 公共 apply 入口：
  - `ActiveCompressionApplyCommandParams`
  - `ActiveCompressionApplyCommandResult`
  - `active_compression_apply`
- dry-run/apply 共用 source session loader：
  - `session_id`：从 `source_provider_id` 对应 provider 原生会话加载。
  - `file`：从 canonical export 文件加载。
  - 同时传 `session_id` 和 `file` 会拒绝，避免来源歧义。
- apply 支持用户选择：
  - `candidate_ids` 为空时压缩全部当前候选。
  - `candidate_ids` 非空时只压缩指定候选。
- apply 输出：
  - 压缩后的 canonical export 文件。
  - `archive_refs`。
  - 完整 `ActiveCompressionReport`。
- 新增 CLI 命令：
  - `memorph compression apply SOURCE TARGET --session-id SESSION_ID`
  - `memorph compression apply SOURCE TARGET --file session.json`
  - `--candidate-id ID` 可重复指定。
  - `--output PREFIX` 指定输出前缀。
  - `--format json|md|html|morph|both` 指定输出格式。
  - 继续支持 dry-run 同款 policy knobs：最近消息保护、最小候选大小、最小节省率。
- 新增 API 路由：
  - `POST /api/v1/compression/apply`
- API body 支持：
  - `source_provider_id`
  - `target_provider_id`
  - `session_id`
  - `file`
  - `policy`
  - `candidate_ids`
  - `output_prefix`
  - `format`
- 新增测试覆盖：
  - core apply 从 canonical file 写出压缩后的 canonical export。
  - apply 写入 archive ref。
  - 压缩后 session 中出现 `EventBlock::Compressed`。
  - 被压缩的旧原文不再作为普通 `EventBlock::Text` 留在压缩后 session。
  - expand 能通过 archive 恢复原始 event。
  - CLI parser 接受 `compression apply` 的 candidate/output/format 参数。
  - API route 对 `session_id` 与 `file` 同时传入的歧义来源返回错误，不写 archive。

明确未做：

- 没有直接修改 Codex/Claude/Kimi/DeepSeek 等 provider 的原生会话文件。
- 没有把 apply 自动接入 session switch。
- 没有 TUI 勾选候选界面。
- 没有 MCP retrieval。
- 没有 LLM summarizer。
- 没有 provider-specific tokenizer。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_apply_from_file_writes_archive_and_expandable_output`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_apply_accepts_candidates_and_output_options`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_apply_route_rejects_ambiguous_source_without_writing_archive`：通过，1 passed。
- `cargo check --manifest-path rust/Cargo.toml -p memorph`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，118 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

遇到的问题：

- core apply 测试最初不能简单断言压缩后 JSON 全文不包含旧关键词。
- 原因：deterministic summary 会保留 preview，preview 中可能合法包含原文片段。
- 处理方式：改为断言旧长文本不再作为普通 `EventBlock::Text` 保存；这更符合真实验收目标，即“原始大内容被压缩块替换，细节进入 archive，而不是直接丢失”。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 8：TUI 用户选择，在界面中展示 candidates、skips、before/after 信息，并允许用户确认 apply。

### 2026-06-07：Phase 8 / TUI 用户选择

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 8 的目标是“在 TUI 展示候选，并让用户选择压缩”。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段边界：接入 TUI action modal，不直接修改 provider 原生会话仓库，不把压缩自动接入 switch，不做 LLM summarizer。

已完成：

- 在 TUI session action modal 中新增 `Compress` 动作。
- `Compress` 动作复用现有 action modal 交互风格，不另起不一致的 wizard。
- 压缩面板支持选择 target agent：
  - target agent 用于 projection-aware compression plan。
  - 切换 target agent 后会重新生成 active compression plan。
- 压缩面板会展示 dry-run plan：
  - candidate 数量。
  - 已选择 candidate 数量。
  - estimated bytes saved。
  - estimated tokens saved。
  - candidate id。
  - candidate kind。
  - risk。
  - source event ids。
  - skipped 事件数量。
- 用户可选择哪些 candidate 要压缩：
  - 默认勾选全部 candidates。
  - 在 `CompressionCandidates` 区域使用左右切换候选。
  - Enter 勾选/取消当前候选。
  - Execute 时只 apply 被勾选的 candidate ids。
- TUI apply 行为：
  - 调用 `active_compression_apply`。
  - 输入来源为当前 provider/session id。
  - 输出为压缩后的 canonical JSON export。
  - 输出路径默认在当前 workspace 下生成 `{session_id}_active_compressed.json`。
  - 返回 applied candidates、estimated savings、output file、archive refs。
- 明确保持安全边界：
  - TUI apply 不直接改 Codex/Claude/Kimi/DeepSeek 等 provider storage。
  - 原文继续写入 durable archive。
  - 目标 provider native projection 仍由后续 export/switch 流程负责。

明确未做：

- 没有把 active compression 自动接入 session switch。
- 没有做 TUI 中的压缩 policy 参数编辑。
- 没有在 TUI 中展示每个 skipped event 的完整明细，只展示 skipped 数量。
- 没有 MCP retrieval。
- 没有 LLM summarizer。
- 没有 provider-specific tokenizer。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib tui::app::tests`：通过，5 passed。
- `cargo check --manifest-path rust/Cargo.toml -p memorph-lib`：通过。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，121 passed。
- `git diff --check`：通过。

遇到的问题：

- 原计划只做“一键压缩全部候选”会弱化用户选择能力。
- 处理方式：本阶段直接补上 candidate 勾选状态，默认全选但允许用户取消任意 candidate，避免 TUI 比 CLI/API 能力退化太多。

提交范围：

- `rust/crates/memorph/src/tui/app.rs`
- `rust/crates/memorph/src/tui/screens/session_list.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 9：Retrieval bridge，提供 CLI/API 级 `memorph-archive://...` 原始内容检索能力，为后续 MCP retrieval 打基础。

### 2026-06-07：Phase 9 / Retrieval bridge

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 9 的目标是提供 CLI/API 级 `memorph-archive://...` 原始内容检索能力。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段边界：只做 durable archive retrieval bridge，不做 MCP server，不做 runtime proxy，不改变压缩 apply/switch 行为。

已完成：

- 新增 core 检索参数和结果结构：
  - `RetrieveCompressionArchiveParams`
  - `RetrievedCompressionArchive`
- 新增 core 检索入口：
  - `retrieve_compression_archive`
- 检索结果直接返回 archive 中的原始内容：
  - `archive_ref`
  - `created_at`
  - `canonical_id`
  - `source_provider_id`
  - `target_provider_id`
  - `summary_event_id`
  - `source_event_ids`
  - `source_event_count`
  - `events`
- 新增 CLI 命令：
  - `memorph compression retrieve ARCHIVE_REF`
  - 输出完整 JSON，便于脚本和后续 MCP bridge 复用。
- 新增 API 路由：
  - `POST /api/v1/compression/retrieve`
  - body：`{ "archive_ref": "memorph-archive://..." }`
  - 返回 `RetrievedCompressionArchive`。
- 将 archive loader 的内部可见性调整为 crate 内可用，便于测试在临时 archive dir 中验证，不改变外部公开 API。
- app crate 显式增加 `serde_json` 依赖，用于 CLI 输出完整结构化 JSON；`Cargo.lock` 仅记录 app crate dependency 列表变化，没有引入新的包版本。

明确未做：

- 没有实现 MCP retrieval tool。
- 没有实现按 query 检索 archive 内部片段。
- 没有把 retrieval 自动注入 provider runtime。
- 没有改变现有 `restore` 行为；`restore` 仍负责写文件，`retrieve` 负责直接返回原始 events。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_accepts_archive_ref`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_apply_from_file_writes_archive_and_expandable_output`：通过，1 passed；该测试新增验证 retrieve 能从 archive 恢复原始 `old-user` event。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_route_rejects_invalid_archive_ref`：通过，1 passed。
- `cargo check --manifest-path rust/Cargo.toml -p memorph`：通过。
- `cargo check --manifest-path rust/Cargo.toml -p memorph-lib`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，123 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

遇到的问题：

- CLI retrieve 最初直接使用 `serde_json::to_string_pretty`，但 `memorph` app crate 没显式依赖 `serde_json`，导致 app 编译失败。
- 处理方式：保留完整 JSON 输出能力，在 app crate 增加 `serde_json = "1"` 显式依赖，而不是降级为不完整的文本摘要。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/core/compression.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/apps/memorph/src/main.rs`
- `rust/apps/memorph/Cargo.toml`
- `rust/Cargo.lock`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 10：LLM summarizer 可选后端，在 deterministic summary 稳定闭环之上增加可选 LLM 摘要，并保持失败 fallback、size gate 和 archive 可逆性。

### 2026-06-07：Phase 10 / 可选 LLM summarizer 接口与安全 gate

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 10 的目标是在 deterministic summary 稳定闭环之上增加可选 LLM summarizer。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段边界：只实现可注入 summarizer 接口、结构化 draft、验证 gate 和 fallback；不直接绑定具体外部模型 API，不引入网络调用，不改变默认 deterministic 行为。

已完成：

- 新增可注入 summarizer trait：
  - `ActiveCompressionSummarizer`
  - `summarize(&CompressionSummaryRequest) -> CompressionSummaryDraft`
- 新增 summarizer 请求结构：
  - `CompressionSummaryRequest`
  - 包含 candidate、source events、archive ref。
- 新增结构化 summarizer draft：
  - `summary`
  - `goals`
  - `decisions`
  - `completed_work`
  - `open_questions`
  - `files`
  - `risks`
- 新增 summary 来源记录：
  - `CompressionSummarySource::Deterministic`
  - `CompressionSummarySource::External`
  - `CompressionSummarySource::DeterministicFallback`
- 新增 summary 拒绝原因：
  - `Empty`
  - `TooLarge`
  - `SummarizerError`
- `CompressionCandidateReport` 现在会在 apply 后记录：
  - `summary_source`
  - `summary_rejection_reason`
- apply 流程保持默认安全：
  - 默认没有 summarizer 时仍使用 deterministic summary。
  - summarizer 只通过显式注入入口启用。
  - archive 写入仍先发生，原始 events 仍完整保存。
  - final summary 中继续包含 `Archive: memorph-archive://...`。
- 外部 summary 必须通过验证：
  - 非空。
  - 实际 summary bytes 小于原始 candidate bytes。
  - 实际 summary bytes 不超过 planner 给出的 compressed budget。
- 外部 summary 失败时自动 fallback：
  - summarizer 报错 fallback deterministic。
  - summarizer 输出过长 fallback deterministic。
  - summarizer 输出空 fallback deterministic。
  - fallback 不影响 archive 写入和 expand 恢复。

明确未做：

- 没有接入具体 LLM provider。
- 没有新增 CLI/API 参数选择 LLM summarizer。
- 没有做 provider-specific 摘要风格。
- 没有做 async/network summarizer。
- 没有改变 TUI 当前默认压缩行为。

这些未做项是刻意保留的后续阶段，因为当前阶段的核心是先把可选 summarizer 后端插槽和安全 gate 做扎实，避免把 LLM 放到不可控的删除/归档路径里。

测试：

- `cargo fmt --manifest-path rust/crates/memorph/Cargo.toml`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression`：通过，12 passed。
- `cargo check --manifest-path rust/Cargo.toml -p memorph-lib`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，126 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- 外部 summarizer 输出足够短时会被使用，`summary_source == External`。
- 外部 summarizer 输出过长时会被拒绝，`summary_source == DeterministicFallback`，`summary_rejection_reason == TooLarge`。
- 外部 summarizer 报错时会 fallback，`summary_rejection_reason == SummarizerError`。
- 使用外部 summary 后，archive expand 仍能恢复原始 event。

遇到的问题：

- 初始测试用的外部 summary 包含过多结构化字段，超过候选压缩预算，被 gate 正确拒绝。
- 处理方式：不放宽 gate，改测试为真正短的外部摘要，证明 gate 是有效边界而不是摆设。

提交范围：

- `rust/crates/memorph/src/core/active_compression.rs`
- `rust/crates/memorph/src/tui/app.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 11：将 active compression 作为可选 switch policy 接入会话切换流程，但必须默认保守，且不能绕过 plan/apply/report/archive/retrieval 的安全闭环。

### 2026-06-07：Phase 11 / 可选 switch active compression policy

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 11 的目标是把 active compression 接入会话切换流程，但必须 opt-in。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段边界：只做 switch 的可选 policy，不改变默认 switch，不把 TUI switch 改成自动压缩，不引入真实 LLM API。

已完成：

- `SwitchParams` 新增可选字段：
  - `active_compression: Option<ActiveCompressionPolicy>`
  - serde 默认缺省为 `None`，序列化时 `None` 不输出。
- `SwitchResult` 新增可选字段：
  - `active_compression_report: Option<ActiveCompressionReport>`
  - 只有 opt-in switch compression 时返回。
- `switch_session` 流程现在支持：
  - source provider scan/import。
  - 如果 `active_compression == None`，保持原来的 imported canonical session 原样进入 `prepare_session_for_export`。
  - 如果 `active_compression == Some(policy)`，先对 imported canonical session 执行 archive-backed active compression。
  - 压缩后的 canonical session 再进入现有 `prepare_session_for_export`，继续复用 target provider projection。
  - source provider 原始 session 不被修改。
- switch opt-in compression 会强制把 policy mode 设为 `Auto`：
  - 原因：switch 阶段没有 candidate 手工选择 UI，显式传入 `--active-compression` 的语义是“切换前应用安全候选压缩”，不是只 plan。
  - candidate 选择仍经过 planner、recent protection、size gate、savings gate 和 archive 写入。
- CLI `switch` 新增显式 opt-in：
  - `memorph switch FROM TO --active-compression`
  - `--protect-recent-message-events N`
  - `--min-candidate-bytes BYTES`
  - `--min-savings-ratio-percent PERCENT`
- CLI policy knobs 必须配合 `--active-compression` 使用，避免用户传了阈值但实际没有启用压缩的静默误解。
- CLI switch 成功后，如果执行了 active compression，会输出同一套 active compression report 摘要和 archive refs。
- API `SwitchBody` 新增：
  - `active_compression: Option<ActiveCompressionPolicy>`
  - body 不传时行为不变。
- TUI switch 明确传 `active_compression: None`：
  - 默认切换仍完全保守。
  - TUI 已有独立 Compress action，用户要压缩时走显式压缩动作。

明确未做：

- 没有把 TUI switch 改成默认压缩。
- 没有在 switch 中加入 candidate 手工选择。
- 没有接入真实 LLM summarizer。
- 没有新增 MCP retrieval。
- 没有改变 provider projection 规则；仍使用现有 canonical compressed block -> target provider native/portable 投影。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib switch_params_omit_active_compression_by_default -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib switch_accepts_active_compression_policy_options -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib switch_active_compression_is_opt_in_and_archive_backed -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，129 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- 默认 `SwitchParams` 序列化时不输出 `active_compression`，证明默认路径仍保守。
- CLI `switch --active-compression` 和 policy 阈值参数可解析。
- switch compression helper 在 `None` 时返回原 session 且无 report。
- switch compression helper 在 `Some(policy)` 时：
  - 产生非 dry-run report。
  - 强制 mode 为 `Auto`。
  - 写入 archive ref。
  - 用 `EventBlock::Compressed` 替换旧大事件。
  - 保留最近受保护事件。

遇到的问题：

- 初始测试直接比较 `Vec<SessionEvent>`，但 `SessionEvent` 没有 `PartialEq`。
- 处理方式：不为了测试扩大 canonical 类型 trait，而是用 serde JSON 等价比较默认不压缩路径，保持变更面更小。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/crates/memorph/src/tui/app.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 12：补 provider-specific token/size policy 或 retrieval bridge 的 provider runtime 暴露。优先建议做 provider-aware token estimate/report，因为当前 active compression gate 仍以字节和粗 token 估算为主，能工作但还不是 Headroom 那种 provider/model tokenizer 级别。

### 2026-06-07：Phase 12 / provider-aware token estimator report

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 12 的优先方向是补 provider-aware token/size policy。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段边界：不引入外部 tokenizer，不调用网络，不声称精确 token；先把 provider-aware heuristic 和 report 暴露出来，替换原来所有场景固定 `bytes / 4` 的估算。

已完成：

- 新增 token estimator report：
  - `CompressionTokenEstimatorReport`
  - `CompressionTokenEstimatorStrategy::ProviderHeuristic`
- `ActiveCompressionReport` 新增：
  - `token_estimator`
- estimator report 当前记录：
  - `strategy`
  - `source_provider_chars_per_token_x100`
  - `target_provider_chars_per_token_x100`
  - `effective_provider_id`
  - `effective_chars_per_token_x100`
- active compression planner 现在按 target provider heuristic 估算 token：
  - session total token estimate
  - candidate original/compressed token estimate
  - skipped event token estimate
  - report saved token estimate
- 当前 provider heuristic：
  - Claude/Anthropic：3.50 chars/token
  - Codex/OpenAI/OpenCode：4.00 chars/token
  - Gemini/Google：3.60 chars/token
  - Kimi：3.20 chars/token
  - DeepSeek：3.30 chars/token
  - unknown：4.00 chars/token
- effective provider 选择 target provider：
  - 原因：active compression 的目标是让迁移后的目标 agent 读取更小上下文，所以 target provider 的上下文计费/估算更重要。
- 保留旧 `estimate_tokens_from_bytes(bytes)` 作为通用 fallback：
  - 兼容旧测试和未来非 provider-aware 调用点。
  - 新 planner 内部使用 `estimate_tokens_from_bytes_with_estimator`。
- CLI active compression report 新增输出 estimator：
  - strategy
  - effective provider
  - chars/token x100

明确未做：

- 没有接入真实 provider tokenizer。
- 没有按具体 model 名称选择 tokenizer。
- 没有改变字节级 savings gate；当前 hard gate 仍以 bytes 为主，token estimate 作为 report 和后续决策基础。
- 没有新增用户可配置 estimator 参数，避免过早暴露不稳定接口。

为什么这不是“半拉子简化”：

- 这一步没有降低压缩安全边界：
  - archive 仍完整保存原文。
  - recent/system/developer/already-compressed 保护仍存在。
  - bytes savings gate 仍存在。
  - summarizer size gate 仍存在。
- 它补的是 Headroom 中“provider/model tokenizer aware”的工程缝隙：
  - 当前先用 provider heuristic 替换固定粗估。
  - report 明确暴露 estimator 来源，避免用户误以为 token 是真实模型 tokenizer 精确值。
  - 后续真实 tokenizer 只需要替换 estimator 函数，不需要重写 planner/apply/switch。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression -- --nocapture`：通过，16 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib toggles_selected_compression_candidate -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，130 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- `dry_run_report_serializes_with_stable_policy_fields` 现在同时验证 `token_estimator` 序列化字段。
- `dry_run_uses_target_provider_token_estimator` 验证同一 session 在不同 target provider 下产生不同 token estimate：
  - Codex 使用 4.00 chars/token。
  - Kimi 使用 3.20 chars/token。
  - Kimi 的 token estimate 高于 Codex。
- TUI 手工 report 测试补上 default estimator，证明新增字段不破坏已有 TUI candidate selection。

遇到的问题：

- `ActiveCompressionReport` 新增字段后，TUI 测试里手工构造 report 需要补字段。
- 处理方式：使用 `CompressionTokenEstimatorReport::default()`，不改变 TUI 运行行为。

提交范围：

- `rust/crates/memorph/src/core/active_compression.rs`
- `rust/crates/memorph/src/tui/app.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 13：优先补 retrieval bridge 的 query/search 能力，向 Headroom CCR 的 `retrieve(hash, query=...)` 靠近。当前 retrieval 已能按 archive_ref 返回完整原始 events，但还不能只取相关片段；做 query/search 后，后续 MCP retrieval 才不会无条件把大 archive 展开回上下文。

### 2026-06-07：Phase 13 / archive retrieval query search

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 13 的目标是补 retrieval bridge 的 query/search 能力。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认本阶段边界：只做 durable archive 内的本地检索，不做 MCP server，不做 LLM rerank，不接外部搜索/向量库。

已完成：

- `RetrieveCompressionArchiveParams` 新增：
  - `query: Option<String>`
  - `max_results: Option<usize>`
- `RetrievedCompressionArchive` 新增：
  - `query`
  - `max_results`
  - `returned_event_count`
  - `matches`
- 新增 `RetrievedCompressionArchiveMatch`：
  - `event_id`
  - `event_index`
  - `score`
  - `snippets`
- retrieve 行为现在分两种：
  - 不传 `query`：保持旧行为，返回 archive 内完整原始 `events`。
  - 传 `query`：只返回命中的 `events`，并返回命中 match/snippet 信息。
- query search 当前使用本地确定性文本检索：
  - 基于 `canonical_event_text(event)`。
  - query 按空白分词。
  - term 命中计数作为基础 score。
  - 完整 query phrase 命中加权。
  - 结果按 score 降序、原始 event 顺序稳定排序。
  - snippets 从命中行提取，最多 3 条，每条最多 240 chars。
- `max_results` 控制 query 模式下最多返回的匹配 events。
- CLI `memorph compression retrieve` 新增：
  - `--query QUERY`
  - `--max-results N`
- API `POST /api/v1/compression/retrieve` body 新增：
  - `query`
  - `max_results`

明确未做：

- 没有实现 BM25。
- 没有实现 embedding/vector search。
- 没有实现跨 archive 搜索。
- 没有实现 MCP retrieval tool。
- 没有自动把 query retrieval 注入 provider runtime。

为什么这一步重要：

- 这是 Headroom CCR 的关键语义补齐：
  - 压缩块里保留 archive ref。
  - 模型或用户需要细节时，不必整段展开 archive。
  - 可以按 query 取回相关片段。
- 这一步避免未来 MCP retrieval 变成“又把大 archive 全量塞回上下文”的反模式。
- 当前实现虽然是 deterministic local search，但已经建立了正确 API 形状；后续 BM25/embedding 只需要替换 scoring，不需要重写 archive/ref/report 结构。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_apply_from_file_writes_archive_and_expandable_output -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_accepts_archive_ref -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_route_rejects_invalid_archive_ref -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，130 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- 无 query retrieve 仍完整返回 archived source event。
- query 命中时：
  - `query` 字段回显。
  - `source_event_count` 保持 archive 原始总数。
  - `returned_event_count` 只统计返回的命中 events。
  - `matches` 返回 event id 和 snippets。
- query 不命中时：
  - `source_event_count` 仍保留总数。
  - `returned_event_count == 0`。
  - `events` 和 `matches` 都为空。
- CLI parser 验证 retrieve 支持 `--query` 和 `--max-results`。

遇到的问题：

- API success route 没有单独造真实 archive fixture，因为当前 archive 存储路径走全局 memorph dir，构造成功路径会引入额外测试环境耦合。
- 处理方式：core 层用临时 archive dir 验证真实 query 行为；API 层保留现有 invalid-ref route 测试，新增字段是直接透传到同一 core params。后续如果补 API archive fixture，可进一步覆盖成功路由。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 14：补 MCP retrieval 工具或 retrieval-provider runtime 暴露。建议先做 MCP retrieval tool，因为 archive query search 已经具备最小可用语义，MCP 工具可以让目标 agent 在看到 `memorph-archive://...` 后按需取回相关片段。

### 2026-06-07：Phase 14 / retrieval tool spec 暴露

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认 Phase 14 的目标是让目标 agent/runtime 能看到 `memorph-archive://...` 后按需检索，而不是无条件展开 archive。
- 已检查当前工作区状态，开始时无未提交改动。
- 已确认项目当前没有现成 MCP server 结构；直接引入 MCP 框架会扩大改动面，不适合本阶段。

本阶段决策：

- 先实现最小、稳定、机器可读的 retrieval tool specification，而不是仓促实现真实 MCP stdio server。
- 原因：
  - 当前已有 `compression retrieve` 和 `/api/v1/compression/retrieve` 的 durable archive query 检索能力。
  - 本阶段缺的是给 agent/runtime 注入工具时可复用的工具契约。
  - tool spec 可以被 CLI、API、后续 MCP server、provider runtime adapter 共同复用。
  - 不复制 retrieve 逻辑，避免后续 MCP/API/CLI 出现参数漂移。

已完成：

- 新增 core 工具契约结构：
  - `CompressionRetrievalToolSpec`
  - `CompressionRetrievalToolApiSpec`
  - `CompressionRetrievalToolCliSpec`
  - `CompressionRetrievalToolOutputContract`
- 新增 core 入口：
  - `compression_retrieval_tool_spec()`
- tool spec 明确：
  - tool name：`memorph_retrieve_compression_archive`
  - archive ref scheme：`memorph-archive://`
  - API 调用方式：`POST /api/v1/compression/retrieve`
  - CLI 调用方式：`memorph compression retrieve <ARCHIVE_REF>`
  - query 调用方式：`--query <QUERY> --max-results N`
  - input schema：`archive_ref` 必填，`query` 和 `max_results` 可选。
  - full retrieval 与 query retrieval 的返回契约差异。
  - 使用规则：不要无条件展开，优先 query retrieval，需要完整原文时才 full retrieval。
- 新增 CLI 命令：
  - `memorph compression tool-spec`
  - 输出完整 JSON tool spec，便于 agent/runtime 读取。
- 新增 API 路由：
  - `GET /api/v1/compression/tool-spec`
  - 返回同一份 core tool spec。

明确未做：

- 没有实现真实 MCP server。
- 没有实现 provider runtime 自动 tool injection。
- 没有实现 tool call response loop。
- 没有新增 retrieve 查询算法；继续复用 Phase 13 的 deterministic archive query search。

为什么这一步不是“半拉子”：

- 本阶段没有把 MCP retrieval 的最终目标降级为“只写文档”。
- 它补的是真实 MCP/server 注入前必须稳定的契约层：
  - tool name。
  - input schema。
  - API/CLI 调用路径。
  - 输出契约。
  - 使用规则。
- 后续做 MCP server 时可以直接复用 `compression_retrieval_tool_spec()`，只新增 stdio/tool-call transport，不需要重新定义参数和语义。
- 更重要的是，本阶段继续强化“不无条件展开 archive”的核心原则。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieval_tool_spec_is_machine_readable_and_query_first -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_tool_spec_command_is_available -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_tool_spec_route_returns_retrieval_contract -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，133 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- core spec 是机器可读的，并且明确 query-first 使用规则。
- CLI parser 支持 `compression tool-spec`。
- API `GET /api/v1/compression/tool-spec` 返回 retrieval contract。

遇到的问题：

- 真实 MCP server 需要先明确项目内 server/transport 位置，否则会引入大而散的架构。
- 处理方式：先把 retrieval tool contract 固化到 core，再由后续阶段接 MCP stdio server 或 provider runtime injection。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 15：基于 Phase 14 的 tool spec 实现真实 MCP stdio server 或 provider runtime tool injection。建议优先做最小 MCP stdio server：暴露 `memorph_retrieve_compression_archive`，调用现有 `retrieve_compression_archive`，支持 `archive_ref/query/max_results`，并测试 query 模式不会全量返回 archive。

### 2026-06-07：Phase 15 / 非 MCP 的 query-first retrieval hint

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认长期目标仍是完善 memorph 压缩机制。
- 已检查当前工作区状态，开始时无未提交改动。
- 用户明确要求：不需要实现 MCP 部分。

本阶段决策：

- 不实现 MCP stdio server。
- Phase 15 改为非 MCP 的 provider runtime 暴露能力：让压缩块的 model-visible 文本明确告诉 agent 如何用 CLI 做 query-first retrieval。
- 原因：
  - Phase 13 已有真实 archive query retrieval。
  - Phase 14 已有机器可读 tool spec。
  - 在不做 MCP 的前提下，目标 agent 最需要的是在 compressed block 里看到可执行的恢复路径。
  - 当前 compressed block 只显示 `Archive: memorph-archive://...`，没有说明“不要全量展开，先按 query 检索”。

已完成：

- 新增共享 helper：
  - `provider::compression_retrieval_hint(archive_ref)`
- portable compressed block projection 现在会在 archive ref 后追加简短提示：
  - `Retrieve specific details with: memorph compression retrieve <ARCHIVE_REF> --query <terms> --max-results 5`
- Codex native `type: "compacted"` model-visible history 同步使用同一条 retrieval hint。
- 这样覆盖：
  - Claude portable projection。
  - Kimi portable projection。
  - DeepSeek portable projection。
  - Cursor/Kiro portable projection。
  - Codex native compacted projection。
  - 其他通过 `canonical_block_text` 输出 compressed block 的 provider。

明确未做：

- 没有实现 MCP。
- 没有实现 provider 自动 tool injection。
- 没有改变 archive/retrieve 数据结构。
- 没有改变 active compression apply/switch 的选择和归档逻辑。
- 没有全量展开 archive。

为什么这一步重要：

- 它补的是非 MCP 场景下的 runtime 可用性：
  - agent 看到 compressed block 时，不再只有 archive ref。
  - agent 会看到 query-first 的具体命令。
  - 这减少把 archive 整段恢复进上下文的概率。
- 它不破坏核心压缩闭环：
  - 原文仍在 durable archive。
  - compressed block 仍保持短摘要。
  - retrieval 仍由 `memorph compression retrieve` 走 Phase 13 的 query search。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_block_text_keeps_provider_projection_concise -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_native_codex_compacted_rollout -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_export_round_trips_as_native_codex_compacted_rollout -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，133 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- portable compressed block 的可见文本包含 query-first retrieval command。
- Codex native compacted projection 相关测试继续通过，证明新增 hint 没破坏 native compaction roundtrip。

遇到的问题：

- 原 Phase 15 计划偏向 MCP，但用户明确不需要 MCP。
- 处理方式：停止 MCP 方向，改做非 MCP 的 retrieval hint，让现有 CLI/API retrieval 真正能被目标 agent 看到和使用。

提交范围：

- `rust/crates/memorph/src/provider.rs`
- `rust/crates/memorph/src/providers/codex.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 16：继续增强非 MCP 的压缩恢复体验。建议补 API/CLI 的 compressed block retrieval examples 或 provider-specific resume instructions，让用户和 agent 能更容易从压缩块定位 archive_ref 并做 query retrieval；仍不做 MCP。

### 2026-06-07：Phase 16 / archive_ref 专用恢复说明

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认当前目标仍是完善压缩机制和恢复体验。
- 已检查当前工作区状态，开始时无未提交改动。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 从 compressed block 中拿到 `memorph-archive://...` 后，用户或 agent 不需要猜怎么恢复。
- 提供 archive_ref 专用的 query-first 恢复说明，包含 CLI 命令和 API body 示例。
- 继续避免把 archive 全量展开回上下文。

已完成：

- 新增 core 结构：
  - `CompressionRetrievalInstructions`
- 新增 core 入口：
  - `compression_retrieval_instructions(archive_ref)`
- instructions 内容包括：
  - `archive_ref`
  - query-first CLI：
    - `memorph compression retrieve <ARCHIVE_REF> --query <terms> --max-results 5`
  - full retrieval CLI：
    - `memorph compression retrieve <ARCHIVE_REF>`
  - query retrieval API body：
    - `{ "archive_ref": "...", "query": "<terms>", "max_results": 5 }`
  - full retrieval API body：
    - `{ "archive_ref": "..." }`
  - suggested steps：
    - 从 compressed block 提取 archive ref。
    - 根据当前问题选窄 query。
    - 优先使用 query retrieval。
    - 只有 query 不够且明确需要完整原始段时才 full retrieval。
- 新增 CLI 命令：
  - `memorph compression instructions ARCHIVE_REF`
  - 输出 JSON instructions，便于用户和 agent 读取。
- 新增 API 路由：
  - `POST /api/v1/compression/instructions`
  - body：`{ "archive_ref": "memorph-archive://..." }`
  - 返回同一份 archive-specific instructions。
- 对无效 archive ref 做拒绝：
  - 不以 `memorph-archive://` 开头会返回错误，避免生成误导说明。

明确未做：

- 没有实现 MCP。
- 没有实现 provider 自动 tool injection。
- 没有读取 archive。
- 没有改变 retrieve 搜索算法。
- 没有改变 compressed block、archive、apply、switch 的核心语义。

为什么这一步重要：

- Phase 15 让 agent 在 compressed block 中看到 query-first 命令。
- Phase 16 让 CLI/API 能基于具体 archive ref 输出完整恢复说明。
- 这补齐了非 MCP 场景下从“看到 archive ref”到“正确按 query 恢复”的操作链路。
- 它继续保持 Headroom CCR 的核心原则：压缩是可逆的，但恢复应该按需、局部、可控。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieval_instructions -- --nocapture`：通过，2 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_instructions_accepts_archive_ref -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_instructions_route -- --nocapture`：通过，2 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，138 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- core instructions 是 archive-specific 的，并且 query-first。
- core instructions 拒绝非法 archive ref。
- CLI parser 支持 `compression instructions ARCHIVE_REF`。
- API route 返回 archive-specific examples。
- API route 拒绝非法 archive ref。

遇到的问题：

- 本阶段没有遇到实现阻塞。
- 注意点：instructions 不读取 archive，只验证 scheme；这是有意设计，因为该能力是“恢复说明生成”，不是 retrieval 本身。真实 retrieval 仍由 Phase 13 的 retrieve API/CLI 执行。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/cli.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/apps/memorph/src/main.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 17：增强 retrieval result 的上下文安全性。建议给 query retrieval 结果增加更明确的 `retrieval_mode` / `recommended_next_action` 字段，帮助 agent 区分 query 局部结果和 full archive 结果，避免把局部检索误判为完整历史。

### 2026-06-07：Phase 17 / retrieval result 上下文安全标记

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认长期目标仍是完善 compression/retrieval 的安全闭环。
- 已检查当前工作区状态，开始时无未提交改动。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 让 retrieval 结果自己说明“这是完整 archive 还是 query 局部结果”。
- 避免 agent 把 query retrieval 的局部片段误判成完整历史。
- 给每种 retrieval mode 返回下一步建议。

已完成：

- `RetrievedCompressionArchive` 新增字段：
  - `retrieval_mode`
  - `recommended_next_action`
- 新增枚举：
  - `CompressionRetrievalMode::FullArchive`
  - `CompressionRetrievalMode::QueryMatches`
  - `CompressionRetrievalMode::QueryNoMatches`
- retrieve 行为现在明确标记：
  - 无 query：`full_archive`
  - query 有命中：`query_matches`
  - query 无命中：`query_no_matches`
- `recommended_next_action` 现在会根据 mode 返回：
  - full archive：这是完整归档段，只把需要部分放回活跃上下文。
  - query matches：这是 query-filtered partial retrieval，不是完整历史。
  - query no matches：建议扩大 query，只有确实需要完整原始段时才 full retrieval。
- `compression_retrieval_tool_spec()` 的 output contract 同步说明：
  - full retrieval 的 `retrieval_mode` 是 `full_archive`。
  - query retrieval 的 `retrieval_mode` 是 `query_matches` 或 `query_no_matches`。

明确未做：

- 没有实现 MCP。
- 没有改变 retrieval 搜索算法。
- 没有改变 archive 存储格式。
- 没有改变 active compression apply/switch 流程。
- 没有改变 CLI/API 参数形状；只是增强返回结果。

为什么这一步重要：

- Phase 13 让 archive 支持 query search。
- Phase 15/16 让 agent/用户知道如何 query-first retrieval。
- Phase 17 让 retrieval response 本身也带上下文安全语义：
  - query 结果是局部，不是完整历史。
  - no-match 不是 archive 空，只是 query 没命中。
  - full archive 结果要谨慎使用，只取必要部分。
- 这进一步贴近 Headroom CCR 的核心原则：retrieve 是按需恢复，不是把压缩内容重新整段塞回上下文。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_apply_from_file_writes_archive_and_expandable_output -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieval_tool_spec_is_machine_readable_and_query_first -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_tool_spec_route_returns_retrieval_contract -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，138 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- full retrieval 返回 `retrieval_mode == FullArchive`，并提示这是完整 archived segment。
- query 命中返回 `retrieval_mode == QueryMatches`，并提示这是 partial retrieval。
- query 不命中返回 `retrieval_mode == QueryNoMatches`，并提示扩大 query。
- tool spec output contract 包含 retrieval mode 语义。

遇到的问题：

- 本阶段没有遇到实现阻塞。
- 注意点：新增字段是返回结果增强，不会影响旧调用参数；旧客户端如果忽略未知 JSON 字段仍可兼容。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 18：补压缩 archive/retrieval 的 API 成功路径 fixture 或端到端 smoke，减少目前 API retrieve 成功路径主要靠 core 临时目录测试覆盖的间接性。

### 2026-06-07：Phase 18 / API retrieve 成功路径 fixture

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是加强 retrieval API 的直接验收证据。
- 已检查当前工作区状态，开始时无未提交改动。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 补上 `/api/v1/compression/retrieve` 的成功路径测试。
- 不只依赖 core 层临时 archive dir 测试间接证明 API 成功路径。
- 验证 API query retrieval 返回的是局部结果，并带 Phase 17 的上下文安全字段。

已完成：

- 在 API 测试模块新增 isolated archive fixture helper：
  - 写入唯一 group 名的 archive。
  - archive 路径位于 memorph 标准 archive 目录下：
    - `~/.memorph/compression_archives/<unique-test-group>/archive.json`
  - fixture 使用 legacy `.json` archive，覆盖生产 loader 支持的 archive 格式。
  - 测试结束后删除该唯一 group 目录。
- 新增 API 成功路径测试：
  - `compression_retrieve_route_returns_query_matches_from_archive`
- 测试 fixture 包含两个 archived events：
  - `needle-event`：包含 query 命中的 `needle detail`。
  - `other-event`：不匹配 query。
- API request：
  - `POST /api/v1/compression/retrieve`
  - body 包含 `archive_ref`、`query: "needle"`、`max_results: 5`。
- 验证 API response：
  - `ok == true`
  - `archive_ref` 回显正确。
  - `retrieval_mode == "query_matches"`
  - `recommended_next_action` 提示 partial retrieval。
  - `source_event_count == 2`
  - `returned_event_count == 1`
  - `events[0].id == "needle-event"`
  - `matches[0].event_id == "needle-event"`
  - snippet 包含 `needle detail`。

明确未做：

- 没有实现 MCP。
- 没有改变 API 参数。
- 没有改变 archive loader。
- 没有改变 query search 算法。
- 没有把测试 archive 写入固定共享文件；使用唯一 group 并清理。

为什么这一步重要：

- Phase 13 已经在 core 层证明 query retrieval 可用。
- Phase 17 已经在 core 层证明 retrieval mode 可用。
- Phase 18 补的是 API 真实 route 成功路径证据：
  - router。
  - JSON body。
  - archive loader。
  - response serialization。
  - retrieval mode/recommended action。
- 这减少了“API retrieve 成功路径只有间接覆盖”的风险。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_route_returns_query_matches_from_archive -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_route_rejects_invalid_archive_ref -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_apply_from_file_writes_archive_and_expandable_output -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，139 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- `/api/v1/compression/retrieve` 成功读取真实 archive fixture。
- API query retrieval 返回局部结果，而不是全量 archive。
- API response 包含 `retrieval_mode` 和 `recommended_next_action`。
- API invalid-ref 路径继续通过。

遇到的问题：

- API route 使用全局 memorph archive dir，而 core test helper 使用临时 archive dir，不能直接复用。
- 处理方式：使用唯一 test archive group 写入标准 archive dir，并在测试结束后清理该唯一目录。避免改动生产 config/path 逻辑，也避免触碰用户已有 archives。

提交范围：

- `rust/crates/memorph/src/api.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 19：补 CLI retrieve 的端到端 smoke 或 JSON 输出验证，确保用户从 compressed block 复制 `memorph compression retrieve ... --query ...` 后能获得包含 `retrieval_mode` 的结构化输出。

### 2026-06-07：Phase 19 / CLI retrieve 端到端 JSON 输出验证

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是补 CLI retrieve 的端到端验收。
- 已检查当前工作区状态，开始时无未提交改动。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 验证用户从 compressed block 中复制：
  - `memorph compression retrieve <ARCHIVE_REF> --query <terms> --max-results 5`
  后，真实 CLI 二进制能输出结构化 JSON。
- 验证 CLI 输出包含 Phase 17 的 `retrieval_mode` 和 `recommended_next_action`。
- 验证 query retrieval 在 CLI 端仍是局部结果，不是全量 archive。

已完成：

- 新增 app crate integration test：
  - `rust/apps/memorph/tests/compression_cli.rs`
- 测试直接运行真实 `memorph` binary：
  - `compression retrieve <ARCHIVE_REF> --query needle --max-results 5`
- 测试 fixture：
  - 使用唯一 group 名写入 archive。
  - archive 路径位于标准 memorph archive 目录下：
    - `~/.memorph/compression_archives/<unique-test-group>/archive.json`
  - fixture 使用 JSON archive，覆盖生产 loader 路径。
  - `Drop` 清理唯一 group 目录，避免遗留测试 archive。
- CLI stdout JSON 验证：
  - `archive_ref` 正确。
  - `retrieval_mode == "query_matches"`。
  - `recommended_next_action` 包含 partial retrieval 提示。
  - `source_event_count == 2`。
  - `returned_event_count == 1`。
  - `events[0].id == "needle-event"`。
  - `matches[0].event_id == "needle-event"`。

明确未做：

- 没有实现 MCP。
- 没有改变 CLI retrieve 行为；仍输出 core retrieve result 的 pretty JSON。
- 没有改变 archive loader。
- 没有改变 retrieve query search 算法。
- 没有新增依赖；fixture 直接写 JSON archive。

为什么这一步重要：

- Phase 15 把 query-first CLI 命令写进 compressed block。
- Phase 16 能为 archive_ref 生成 query-first instructions。
- Phase 17 给 retrieval result 加了上下文安全标记。
- Phase 18 验证 API route 成功路径。
- Phase 19 验证最终用户/agent 实际执行 CLI 命令时能拿到同样的安全 JSON 输出。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph compression_retrieve_cli_outputs_query_mode_json -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，139 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph`：通过，4 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- 真实 `memorph` binary 的 `compression retrieve` query 模式。
- CLI stdout JSON 可被解析。
- CLI stdout 包含 `retrieval_mode`、`recommended_next_action`、局部 event/match。

遇到的问题：

- 为避免给 app crate 增加额外依赖，integration test 没有构造 Rust archive struct，而是直接写生产 loader 可解析的 archive JSON。
- 这保持了测试轻量，同时仍然覆盖真实 CLI binary、archive loader 和 stdout JSON。

提交范围：

- `rust/apps/memorph/tests/compression_cli.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 20：评估是否需要补 retrieval archive fixture 的清理/隔离工具，或开始补更强的 query scoring/BM25。建议优先做轻量清理/隔离工具，保证持续测试不会污染用户 archive 目录。

### 2026-06-07：Phase 20 / retrieve 结果覆盖范围显式化

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段仍属于 Headroom CCR 的 retrieve/restore 能力完善。
- 已检查当前工作区状态，开始时只有 Phase 20 相关待收束改动。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 让 retrieval response 显式说明本次到底返回了哪些 archived events。
- 让 query retrieval 显式说明还有多少 archived events 没有被返回。
- 避免 agent 把 query partial retrieval 误认为完整历史，从而错误恢复或污染目标 agent 上下文。

已完成：

- `RetrievedCompressionArchive` 新增：
  - `returned_event_ids: Vec<String>`
  - `omitted_event_count: usize`
- full retrieval 行为：
  - `returned_event_ids` 包含完整 archive events 的 id。
  - `omitted_event_count == 0`。
- query match 行为：
  - `returned_event_ids` 只包含命中的 event id。
  - `omitted_event_count` 表示 archive 中未被本次 query 返回的 event 数量。
- query no-match 行为：
  - `returned_event_ids` 为空。
  - `omitted_event_count == source_event_count`。
- retrieval tool spec 的 output contract 同步补充这两个字段语义。
- API retrieve 成功路径测试同步验证两个字段。
- CLI retrieve 端到端 JSON 输出测试同步验证两个字段。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有改变 CLI/API 请求参数。
- 没有改变 archive loader。
- 没有改变 query search/scoring 算法。
- 没有改变默认压缩策略或压缩阈值。
- 没有自动展开更多历史；本阶段只是让返回结果更可审计。

为什么这一步重要：

- Phase 17 已经告诉 agent 当前是 `full_archive`、`query_matches` 还是 `query_no_matches`。
- Phase 20 进一步告诉 agent：
  - 当前拿到了哪些具体 source events。
  - 还有多少 source events 没拿到。
- 这符合用户要求的“不能把已经压缩过的无意义内容无条件展开到另一个 agent”：
  - agent 可以优先用局部命中内容回答。
  - 如果信息不足，可以根据 omitted count 决定是否扩大 query。
  - 不需要默认把完整 archive 灌回上下文。
- 这也贴近 Headroom CCR 的 retrieve discipline：retrieve 是按需恢复，不是无条件解压。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_apply_from_file_writes_archive_and_expandable_output -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_route_returns_query_matches_from_archive -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph compression_retrieve_cli_outputs_query_mode_json -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，139 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph`：通过，4 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- core full retrieval 的 `returned_event_ids` 和 `omitted_event_count`。
- core query match 的 `returned_event_ids` 和 `omitted_event_count`。
- core query no-match 的空 `returned_event_ids` 和完整 omitted count。
- API retrieve route 的 query partial coverage metadata。
- 真实 CLI binary 的 query partial coverage metadata。

遇到的问题：

- 本阶段没有遇到实现阻塞。
- 注意点：这是返回 JSON 增强。旧客户端如果严格校验 response schema，可能需要同步接受新增字段；正常忽略未知字段的客户端不受影响。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/api.rs`
- `rust/apps/memorph/tests/compression_cli.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 21：建议优先做 retrieval archive fixture 的共享隔离/清理工具，降低 API/CLI 测试重复和污染用户 archive 目录的风险；仍然不做 MCP。

### 2026-06-07：Phase 21 / API retrieve 测试 archive 清理 guard

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段是测试隔离/清理可靠性，不改变压缩功能本身。
- 已检查当前工作区状态，Phase 20 已提交，开始时工作区干净。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 降低 API retrieve 成功路径测试污染用户 archive 目录的风险。
- 保持测试 fixture 仍使用真实 memorph archive loader 路径。
- 避免为了共享 fixture 过早引入 `test-support` feature 或独立测试支持 crate。

已完成：

- API 测试新增 `ArchiveFixture` guard：
  - 持有 `archive_ref`。
  - 持有唯一 `group_dir`。
  - `Drop` 时执行 `remove_dir_all(group_dir)`。
- `compression_retrieve_route_returns_query_matches_from_archive` 改为使用 guard。
- 删除测试中手写的显式清理语句，避免 route/panic 早退时遗漏清理。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有改变生产 archive loader。
- 没有改变 API/CLI 请求或返回协议。
- 没有强行把 API test helper 暴露给 CLI integration test。
- 没有新增 `test-support` feature；当前收益不足以引入额外架构。

为什么这一步重要：

- Phase 18 和 Phase 19 都需要在标准 memorph archive 目录下写真实 archive，才能覆盖生产 loader。
- CLI 测试已经有 RAII cleanup；API 测试此前是手动 cleanup。
- Phase 21 让 API/CLI 两边的清理模型一致：
  - 正常路径会清理。
  - 断言 panic/unwind 时也尽量清理。
  - 不触碰用户已有 archives，因为测试 group 使用唯一前缀。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieve_route_returns_query_matches_from_archive -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，139 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- 本阶段没有新增行为测试；目标是强化已有 API retrieve 成功路径测试的隔离和清理可靠性。

遇到的问题：

- API unit test helper 不能被 CLI integration test 直接复用，除非引入 feature-gated test-support API 或独立测试 crate。
- 本阶段选择不引入该抽象，因为当前重复很小，且用户要求核心功能不要被半拉子抽象拖慢。

提交范围：

- `rust/crates/memorph/src/api.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 22：建议开始补 query retrieval 的排序/匹配质量测试，先把当前朴素 search 的行为边界固定下来，再决定是否引入 BM25/term scoring；仍然不做 MCP。

### 2026-06-07：Phase 22 / query retrieval 排序边界测试

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是固定 retrieve query search 的行为边界。
- 已检查当前工作区状态，Phase 21 已提交，开始时工作区干净。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 把当前 archive query search 的排序规则用测试固定下来。
- 为后续是否引入 BM25/更强 term scoring 提供回归基线。
- 不在没有必要时改算法，避免把检索质量问题和行为变更混在一个阶段。

当前已固定的 search 行为：

- query 会按 ASCII lowercase 做大小写无关匹配。
- query 会按 whitespace 拆 term。
- 每个 term 在 event 可见文本中的出现次数累加为基础 score。
- 完整 query phrase 命中时额外加分。
- score 为 0 的 event 不返回。
- 返回结果按 score 降序。
- 同分时按 source archive 中原始 event 顺序升序。
- `max_results` 会截断返回的 events/matches。
- snippets 优先返回包含 query phrase 或任意 term 的行。

已完成：

- 新增 core 测试：
  - `archive_query_search_ranks_phrase_and_term_frequency_deterministically`
  - 验证 term frequency 更高的 event 排在 phrase 命中 event 前面。
  - 验证 `max_results` 截断低分 event。
  - 验证返回 events 和 matches 顺序一致。
  - 验证 snippet 包含命中的 query 内容。
- 新增 core 测试：
  - `archive_query_search_keeps_source_order_for_equal_scores`
  - 验证相同 score 下保持原 source event 顺序。
  - 验证 match 的 `event_index` 仍指向原 archive 位置。
- 新增测试辅助：
  - `archive_search_event(...)`
  - 只用于构造 query search 单测事件。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有改 query search 算法。
- 没有引入 BM25。
- 没有引入新依赖。
- 没有改变 CLI/API/core 返回协议。

为什么这一步重要：

- 之前 retrieval query 可用，但排序规则没有专门被测试约束。
- 后续如果引入 BM25、字段权重、role 权重或 snippet 改进，需要知道哪些语义是故意改变，哪些是不能破坏。
- 对用户目标而言，跨 agent 压缩恢复不能只做到“能搜到”，还要做到“可预测、可解释、不会乱展开”。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib archive_query_search_ranks_phrase_and_term_frequency_deterministically -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib archive_query_search_keeps_source_order_for_equal_scores -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，141 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- query search score 排序。
- phrase bonus 与 term frequency 的当前相对行为。
- `max_results` 截断。
- 同分稳定排序。
- snippets 基本命中内容。

遇到的问题：

- 当前算法不是 BM25，只是 deterministic term-frequency + phrase bonus。
- 本阶段刻意不改算法，因为没有先固定回归基线就直接换算法，后续很难判断检索行为变化是否合理。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 23：基于 Phase 22 的基线，评估是否做一个不引入依赖的轻量 scoring 改进，优先考虑“更明确的 phrase 优先/term 覆盖率/短噪声惩罚”，仍然不做 MCP。

### 2026-06-07：Phase 23 / query retrieval 轻量 scoring 改进

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是基于 Phase 22 的检索排序基线做一个小而可验证的质量改进。
- 已检查当前工作区状态，Phase 22 已提交，开始时工作区干净。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 改善 archive query retrieval 的排序质量。
- 避免单个词重复出现的噪声事件压过更完整、更贴近 query 的事件。
- 保持实现轻量、确定性、无新依赖。

已完成：

- 将 query terms 做去重，避免重复 query word 人为抬高 score。
- 新增内部 scoring helper：
  - `archive_query_score(...)`
- 新 scoring 规则：
  - 没有任何 term 命中时 score 为 0，不返回。
  - 每个不同命中 term 给基础分。
  - 每个 term 的出现次数只做 capped bonus，避免长日志里单词刷屏污染排序。
  - 命中所有 query terms 时给 coverage bonus。
  - 命中完整 query phrase 时给最高优先级 bonus。
  - 同分仍然按原 archive event 顺序稳定排序。
- 更新 Phase 22 的排序测试，使其反映新的 intentional behavior：
  - exact phrase 应排在散落 term 前。
  - term coverage 应排在单 term repetition 前。
  - equal score 时仍保持 source order。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有引入 BM25。
- 没有引入新依赖。
- 没有改变 archive 格式。
- 没有改变 CLI/API 参数。
- 没有改变 retrieval response schema。
- 没有调用 LLM。

为什么这一步重要：

- 用户要求压缩恢复不能“没底线没有标准地简化”，也不能把无意义压缩内容无条件展开。
- query-first retrieval 的质量决定了 agent 是否能只取必要上下文。
- 如果 query search 只按 term frequency 排序，长日志或重复词很容易挤掉真正相关的压缩历史。
- Phase 23 让检索更接近实际使用需求：
  - 精确短语优先。
  - 覆盖更多 query terms 优先。
  - 单词重复只作为弱信号。
  - 排序仍可解释、可测试、可维护。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib archive_query_search_ranks_phrase_before_repeated_scattered_terms -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib archive_query_search_ranks_term_coverage_before_single_term_repetition -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib archive_query_search_keeps_source_order_for_equal_scores -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，142 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph`：通过，4 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- exact phrase 排序优先。
- term coverage 排序优先。
- 单 term repetition 不再压过覆盖更多 query terms 的 event。
- 同分稳定排序保持不变。

遇到的问题：

- 没有引入 BM25 是刻意选择：
  - 当前 archive retrieval 数据规模通常不大。
  - 引入依赖和 IDF corpus 语义会扩大改动面。
  - 这一阶段先解决最明显的排序噪声问题。
- 如果后续 archive 很大或 query 质量要求更高，再进入 BM25/field weighting 阶段。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 24：建议补 retrieval query 的 field/source 可解释性，例如在 match 中增加命中的 snippet 已足够，但是否需要暴露 `score_reason` 要谨慎评估；优先不改 response schema，先看现有 UI/CLI 是否已经够用。

### 2026-06-07：Phase 24 / query scoring guidance 暴露但不改 retrieve schema

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是提升 query retrieval 的可解释性。
- 已检查当前工作区状态，Phase 23 已提交，开始时工作区干净。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段判断：

- 暂不在 retrieve response 中增加 `score_reason`。
- 原因：
  - 这会改变 retrieve response schema。
  - 当前 `matches.score + snippets + retrieval_mode + returned_event_ids + omitted_event_count` 已经能支撑 agent 做 query-first 判断。
  - 更低风险的做法是先把 scoring 语义放进现有 tool spec / instructions guidance。

已完成：

- 在 `compression_retrieval_tool_spec().usage_rules` 中新增 scoring 说明：
  - query score 优先 exact phrase。
  - 其次优先 distinct query terms coverage。
  - repeated single-term hits 只是弱证据。
- 在 `compression_retrieval_instructions(...).suggested_steps` 中新增使用建议：
  - 多个 match 返回时优先高分。
  - score 偏向 exact phrases 和 broader term coverage。
  - 不要把 repeated single-term noise 当作强相关。
- core tests 增加断言，确保 tool spec 和 instructions 不会丢失 scoring guidance。
- API route tests 增加断言，确保 `/api/v1/compression/tool-spec` 和 `/api/v1/compression/instructions` 也暴露这条 guidance。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有改变 retrieve response schema。
- 没有新增 `score_reason` 字段。
- 没有改变 archive 格式。
- 没有改变 CLI/API 参数。
- 没有调用 LLM。

为什么这一步重要：

- Phase 23 改进了 query scoring，但 agent 如果不知道 score 大致代表什么，仍可能误用结果。
- Phase 24 让 agent 能正确理解：
  - 高分通常意味着 phrase/coverage 更强。
  - 重复单词不是强证据。
  - query retrieval 仍然是局部恢复，不是完整展开。
- 这符合用户的核心要求：
  - 不无条件展开压缩内容。
  - 不把无意义旧内容塞给目标 agent。
  - 压缩和恢复有明确规则，而不是黑箱简化。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieval_tool_spec_is_machine_readable_and_query_first -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_retrieval_instructions_are_archive_specific_and_query_first -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_tool_spec_route_returns_retrieval_contract -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compression_instructions_route_returns_archive_specific_examples -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，142 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- core tool spec scoring guidance。
- core archive-specific instructions scoring guidance。
- API tool spec route scoring guidance。
- API instructions route scoring guidance。

遇到的问题：

- `score_reason` 可能有价值，但本阶段不做。
- 如果后续 UI/agent 需要更强解释性，可以单独做 schema 变更，并同时补 CLI/API compatibility tests。

提交范围：

- `rust/crates/memorph/src/core.rs`
- `rust/crates/memorph/src/api.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 25：建议检查压缩块投影到各 provider 时是否都能携带 query-first retrieval hint 和 archive_ref，尤其是非 native portable providers；仍然不做 MCP。

### 2026-06-07：Phase 25 / provider projection retrieval hint 覆盖

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是检查压缩块导出到各 provider 时是否保留 archive_ref 和 query-first retrieval hint。
- 已检查当前工作区状态，Phase 24 已提交，开始时工作区干净。
- 已继续遵守用户要求：不实现 MCP 部分。

本阶段目标：

- 确认 compressed block 投影到不同 agent/provider 时，不会丢失 `archive_ref`。
- 确认 portable projection 的模型可见文本包含 query-first retrieve 命令。
- 确认 native projection 至少有 machine-readable archive metadata；Codex native 还应在 replacement history 中携带 query-first hint。

已完成：

- 审计发现：
  - 通用 `canonical_block_text(EventBlock::Compressed)` 已会输出：
    - compressed marker。
    - summary。
    - source event count。
    - archive ref。
    - `memorph compression retrieve ... --query <terms> --max-results 5`。
  - Claude/DeepSeek/Kimi/Kiro/Cursor/Codex fallback 都复用 portable text 路径，但测试此前没有显式锁定 query-first hint。
  - Codex native compacted rollout 的 replacement history 已包含 query-first hint，但测试此前没有显式锁定。
  - OpenCode native compaction part 已保留 `archiveRef`，但没有额外 `retrievalHint` metadata。
- 为 OpenCode native compaction part 增加：
  - `memorph.retrievalHint`
  - 值为 query-first CLI hint。
- 补充 provider projection 测试断言：
  - Claude portable text block 包含 query-first retrieve command。
  - DeepSeek portable message content 包含 query-first retrieve command。
  - Kimi portable text part 包含 query-first retrieve command。
  - Kiro portable history text 包含 query-first retrieve command。
  - Cursor portable bubble text 包含 query-first retrieve command。
  - Codex fallback portable content 包含 query-first retrieve command。
  - Codex native compacted replacement history 包含 query-first retrieve command。
  - Codex active compression native rollout 中真实生成的 archive_ref 也出现在 query-first retrieve command 中。
  - OpenCode native compaction metadata 包含 `archiveRef` 和 `retrievalHint`。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有自动展开 archive。
- 没有改变 archive 格式。
- 没有改变 CLI/API 参数。
- 没有改变 retrieve response schema。
- 没有把 old source event ids 泄漏进 portable text。

为什么这一步重要：

- 用户核心要求是“在 A 里压缩的内容，迁移到 B 时不能被 B 无条件展开，也不能被当成普通无意义上下文读进去”。
- provider projection 是跨 agent 迁移时最后一道关口。
- 如果 portable/native projection 丢失 archive_ref 或 query-first hint，目标 agent 就只能看到摘要，无法按需恢复；或者更糟，会把压缩块当作普通文本处理。
- Phase 25 锁定了跨 provider 的最低契约：
  - 看得到这是压缩块。
  - 看得到 archive_ref。
  - 知道应该 query-first retrieve。
  - 不会默认携带完整旧事件。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_portable_claude_text_block -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_portable_deepseek_message_content -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_portable_kimi_text_part -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_portable_kiro_text -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_portable_cursor_bubble_text -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_content_fallback_stays_portable_for_non_native_paths -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_native_codex_compacted_rollout -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib active_compression_export_round_trips_as_native_codex_compacted_rollout -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib compressed_segment_exports_as_native_opencode_compaction -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，142 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph`：通过，4 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- portable provider query-first hint。
- Codex native query-first hint。
- OpenCode native machine-readable retrieval hint。
- active compression 真实 archive_ref 在 Codex native compacted history 中可被 query-first retrieve。

遇到的问题：

- OpenCode native 旧测试 fixture 使用 `archive://s1`，不适合验证 memorph retrieve CLI。
- 本阶段将该测试 fixture 改为 `memorph-archive://s1/archive.json.gz`，更贴近 memorph 当前 archive 机制。

提交范围：

- `rust/crates/memorph/src/providers/*` 中受影响 provider 测试。
- `rust/crates/memorph/src/providers/opencode.rs`：新增 native compaction `retrievalHint` metadata。
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 26：建议检查 portable compressed text 的 import/normalize 是否会保留 retrieval hint 之外的 archive_ref，同时不要把 hint 当成 summary 污染；仍然不做 MCP。

### 2026-06-07：Phase 26 / portable hint normalize 不污染 summary

状态：完成。

本次开始前已执行：

- 已重新阅读本文件，确认本阶段目标是检查 portable compressed text 的 import/normalize 行为。
- 已检查当前工作区状态，Phase 25 已提交，开始时工作区干净。
- 已继续遵守用户要求：不实现 MCP 部分。

发现的问题：

- Phase 25 让 portable compressed text 明确携带 query-first retrieval hint。
- 但 `parse_portable_compressed_text(...)` 旧逻辑只会跳过：
  - `Source event count: ...`
  - `Archive: ...`
- 因此 portable text 从非 native provider 再 normalize 回 `EventBlock::Compressed` 时，`Retrieve specific details with: ...` 会被误当成 summary 的一部分。

本阶段目标：

- 保留 archive_ref。
- 保留 source event count。
- 保留真正的 summary。
- 不把 retrieval hint 行写入 compressed summary。
- 不改变 portable text 输出格式。

已完成：

- 新增内部 helper：
  - `is_portable_retrieval_hint_line(...)`
- `parse_portable_compressed_text(...)` 现在会跳过 memorph 生成的 retrieval hint 行：
  - `Retrieve specific details with: memorph compression retrieve ...`
- 更新测试 `preserve_recovers_portable_compressed_text_from_non_native_provider`：
  - portable fixture 中加入 query-first retrieval hint。
  - 断言 normalize 后的 `summary == "portable summary"`。
  - 断言 `archive_ref == "memorph-archive://portable/a.json"`。
  - 断言 source event count 仍为 42。

明确未做：

- 没有实现 MCP。
- 没有新增 MCP tool。
- 没有改变 archive 格式。
- 没有改变 CLI/API 参数。
- 没有改变 retrieve response schema。
- 没有改变 provider portable text 输出格式。
- 没有自动展开 archive。

为什么这一步重要：

- retrieval hint 是控制恢复方式的元信息，不是压缩摘要语义。
- 如果 hint 混入 summary，跨 agent 再迁移时会出现摘要污染：
  - 目标 agent 看到的 summary 更长。
  - 压缩块可能重复携带恢复命令。
  - 后续二次压缩/投影会把指导语当作原上下文内容。
- Phase 26 确保 portable hint 能帮助恢复，但不会破坏 canonical compressed block 的语义边界。

测试：

- `cargo fmt --all`：通过。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib preserve_recovers_portable_compressed_text_from_non_native_provider -- --nocapture`：通过，1 passed。
- `cargo test --manifest-path rust/Cargo.toml -p memorph-lib`：通过，142 passed。
- `cargo check --manifest-path rust/Cargo.toml`：通过。
- `git diff --check`：通过。

新增测试覆盖：

- portable compressed text 带 retrieval hint 时 normalize 成 `EventBlock::Compressed`。
- retrieval hint 不进入 summary。
- archive_ref 和 source_event_count 不丢失。

遇到的问题：

- 本阶段实际修复了 Phase 25 后暴露出的 normalize 污染问题。
- 这是预期内的后续收口，不影响 Phase 25 的 provider projection 契约。

提交范围：

- `rust/crates/memorph/src/core/compression.rs`
- `memorph基于headroom完善压缩机制。md`

下一步：

- 进入 Phase 27：建议补 portable compressed text 多行 summary + metadata line 的解析边界测试，确保 summary trimming 和 metadata skipping 组合稳定；仍然不做 MCP。
